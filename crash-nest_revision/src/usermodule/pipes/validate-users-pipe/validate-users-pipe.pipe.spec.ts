import { ValidateUsersPipePipe } from './validate-users-pipe.pipe';

describe('ValidateUsersPipePipe', () => {
  it('should be defined', () => {
    expect(new ValidateUsersPipePipe()).toBeDefined();
  });
});
